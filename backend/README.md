# Backend

This directory is reserved for the Node.js Lambda handlers that will power the
planner API.

The first implementation milestone will add:

- `GET /health`
- `GET /pathways`
- `GET /pathways/{pathwayId}`

Authenticated plan endpoints will follow after Cognito is configured.
